name = "aurora-gemini-relay"
main = "worker.js"
compatibility_date = "2026-08-01"

[observability]
enabled = true

# أضف المفتاح من لوحة Cloudflare:
# Workers > aurora-gemini-relay > Settings > Variables > GEMINI_API_KEY
