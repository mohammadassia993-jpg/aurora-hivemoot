# OPPORTUNITY_DEDUP — منع تكرار الفرص

**التاريخ:** 2026-09-13 | **المهمة:** 1.2 — حذف الفرص المكررة

## الآلية
- لكل فرصة يُحفظ `external_id` فريد في قاعدة البيانات (م.field `external_id` في جدول `opportunities`).
- عند إضافة فرصة جديدة، يتحقق النظام من عدم وجود `external_id` مطابق.
- إذا وُجدت مسبقاً، لا تُرسل مرة أخرى.
- مدة إعادة الإرسال: 7 أيام كحد أدنى.

## التنفيذ
- في `src/scheduler.js` — فحص `external_id` قبل الإدراج.
- في `src/tasks-to-products.js` — فحص مزدوج قبل إنشاء مهمة.
- سجل التدقيق: `data/production/`.

## المخرَج
✅ لا فرصة مكررة تصل للقائد.
