# OPPORTUNITIES_REAL.md — التحقق من الفرص واستبدال الوهمي بالحقيقي
التاريخ: 2026-09-13 05:55 UTC

---

## 🔍 نتيجة الفحص (الأمر السابق كان محقاً)
- الفرص المولّدة من `research.js` (external_id بادئته `research:`) هي **من خيال AI** بدون روابط/تحقق — كانت 8 صفوف في قاعدة البيانات.
- مصدرها: `runDailyResearch/runWeeklyResearch` تطلب JSON من AI ثم تُدخل النتائج كفرص دون تحقق (وحتى من الـ simulation عند غياب المفاتيح).

## ✅ ما تم
1. **إزالة آلية التوليد الوهمي**: `research.js` لم يعد يُدخل أي فرصة من AI — صفوف `research:%` تُحذف تلقائياً عند كل فحص.
2. **كتابة ماسح حقيقي** `src/opportunity-scan.js` بترميز واضح:
   - RemoteOK API (مؤكد 200) — تصفية Web3/Blockchain/DeFi.
   - Remotive API (مؤكد 200) — تصفية Web3/Crypto.
   - التحقق من `url` الحقيقي + `external_id` فريد لكل مصدر (يمنع التكرار).

## ⚠️ ملاحظة فنية صادقة حول المصدرين المذكورين في الأمر
- `web3.career/rss` و`/feed` و`/rss.xml` → **404** (لا RSS عام حالياً).
- Immunefi: لا توجد نقطة API عامة (جميع مسارات /api/* -> 404؛ الموقع React بدون __NEXT_DATA__).
- لذلك اعتمدنا **RemoteOK + Remotive** (حقيقيان ومؤكدان الآن) كبديل يعمل، مع إمكانية إضافة Immunefi/Web3Career مستقبلاً عند توفرهما.

## ✅ اختبار حي للوصول
- `scanRealOpportunities()` محلياً: **8 فرص حقيقية** (3 RemoteOK + 5 Remotive) بروابط فعلية، وأُدخلت بصفتها `jobs`/`discovered`.

## ✅ الحالة: لا فرص وهمية — الماسح يستعمل مصادر حقيقية فقط.
