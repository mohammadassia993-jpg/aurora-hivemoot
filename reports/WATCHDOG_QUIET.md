# WATCHDOG_QUIET.md — كتم تقارير "استعادة العافية" الكاذبة

**التاريخ:** 2026-09-14 | **المهمة:** 1.1

## التعديلات في `src/watchdog.js`
1. **شرط التنبيه:** تنبيه واحد فقط بعد **3 فشل متتالي حقيقي** (لا قبله).
2. **فاصل التنبيهات:** 30 دقيقة بين كل تنبيه وآخر (`ALERT_COOLDOWN_MIN = 30`).
3. **الأخطاء المؤقتة:** `timeout/network/ECONN/ECONNRESET/aborted` **لا تُحسب** في عدّاد الفشل ولا تطلق أي تنبيه.
4. **رسائل الاستعادة:** تُرسل فقط إذا كان الانقطاع قد أنذر فعلاً (`alerted=true`) — لا رسالة "استعاد عافيته" لضوضاء لم تنبّه.

### آلية `trackHealth` الجديدة
- baseline أول تشغيل: تسجيل بدون تنبيه.
- صحيحة ⇒ تصفير العدّاد (+ تنبيه استعادة فقط عند إنذار سابق).
- غير صحيحة وعابرة ⇒ تُسجَّل ولا تُحسب.
- غير صحيحة ودائمة ⇒ increment؛ عند 3 ⇒ تنبيه ثم cooldown 30 دقيقة.

## الاختبارات
`test/quiet-filters.test.js` — 4 اختبارات للـ watchdog (10/10 إجمالي):
- عابر لا يُحتسب ✅
- أقل من 3 لا يُنبّه ✅
- الثالث فقط يُنبّه ✅
- الاستعادة بعد تنبيه تصفّر العدّاد ✅

## المخرج
✅ لا رسائل "استعادة عافية" كاذبة — تنبيه واحد فقط بعد 3 فشل متتالي حقيقي + 30 دقيقة فاصل.

---
فريق عمالقة الصمت
